# GPU Support: This project supports both NVIDIA (CUDA) and AMD (ROCm) GPUs
#
# For NVIDIA GPUs (CUDA):
#   pip install -r requirements_py313.txt
#
# For AMD GPUs (ROCm):
#   pip install -r requirements_rocm.txt
#
# Python 3.13 Compatible Requirements
# Some packages require latest versions for Python 3.13 support

torch>=2.0.0
numpy>=1.26.0  # Python 3.13 requires newer numpy
pandas>=2.1.0  # Python 3.13 compatibility
gymnasium>=0.29.0
tensorboard>=2.14.0
wandb>=0.16.0
matplotlib>=3.8.0  # Updated for Python 3.13
seaborn>=0.13.0  # Updated for Python 3.13
tqdm>=4.66.0
pyyaml>=6.0.1
psutil>=5.9.0
