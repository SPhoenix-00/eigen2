# ROCm Requirements for Project Eigen 2
# For AMD GPUs (Radeon RX 6000/7000 series, Instinct MI series)
#
# Installation instructions:
#   1. Install ROCm drivers from: https://rocm.docs.amd.com/
#   2. Upgrade pip: python -m pip install --upgrade pip
#   3. Install PyTorch with ROCm support:
#      python -m pip install -r requirements_rocm.txt
#
# Supported AMD GPUs:
#   - Consumer: RX 6800, 6900 XT, 7900 XT, 7900 XTX (RDNA2/RDNA3)
#   - Datacenter: MI100, MI200, MI250, MI300 series
#
# Recommended ROCm versions: 5.7 or 6.0+
# Note: Updated for Python 3.13 compatibility and numpy 2.x support

# PyTorch with ROCm 6.0 support (better Python 3.13 compatibility)
--index-url https://download.pytorch.org/whl/rocm6.0
torch>=2.5.0

# Core dependencies - versions compatible with numpy 2.x
numpy>=1.26.0,<2.3.0
pandas>=2.2.0
scipy>=1.11.0
numba>=0.58.0

# RL and ML libraries
gymnasium>=0.29.0
tensorboard>=2.14.0
wandb>=0.18.0

# Visualization
matplotlib>=3.8.0
seaborn>=0.13.0

# Utilities
tqdm>=4.66.0
pyyaml>=6.0.1
psutil>=5.9.0

# ROCm-specific notes:
# - torch.cuda.* API works transparently with ROCm
# - GradScaler and autocast work with ROCm backend
# - Model checkpoints are cross-compatible with CUDA
# - Performance should be comparable to NVIDIA GPUs of similar class
#
# Performance expectations:
#   RX 7900 XTX ~ RTX 4080
#   MI250X ~ A100 80GB
#
# Environment variables (automatically set by device_utils.py):
#   PYTORCH_HIP_ALLOC_CONF=expandable_segments:True
#   HSA_FORCE_FINE_GRAIN_PCIE=1
#
# If you encounter dependency conflicts:
#   1. Create a fresh virtual environment
#   2. Install in this order:
#      python -m pip install torch --index-url https://download.pytorch.org/whl/rocm6.0
#      python -m pip install numpy scipy numba
#      python -m pip install -r requirements_rocm.txt
