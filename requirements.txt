# Project Eigen 2 Requirements
# =============================
#
# For Runpod ROCm deployments (runpod/pytorch:2.4.0-py3.10-rocm6.1.0-ubuntu22.04):
#   - PyTorch is pre-installed with ROCm support, DO NOT reinstall
#   - Just run: pip install -r requirements.txt
#
# The codebase automatically detects CUDA or ROCm backends.

# NOTE: torch is NOT listed here - use the pre-installed ROCm PyTorch from the base image
numpy>=1.24.0
pandas>=2.0.0
gymnasium>=0.29.0
tensorboard>=2.14.0
wandb>=0.16.0
matplotlib>=3.7.0
seaborn>=0.12.0
tqdm>=4.65.0
pyyaml>=6.0
psutil>=5.9.0
pydantic>=2.0.0
openpyxl>=3.1.0
google-cloud-storage>=2.10.0